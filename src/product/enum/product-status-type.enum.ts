export enum ProductStatusType {
  /**
   * The product is being sold and can be requested
   */
  CURRENT = 1,
  /**
   * The product is no longer sold and cannot be requested
   */
  OBSOLETE = 2,
}
